@model GestorContatos.Models.Telefone

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Telefone</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.CodFone)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.CodFone)
            @Html.ValidationMessageFor(model => model.CodFone)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.CodContato, "Contato")
        </div>
        <div class="editor-field">
            @Html.DropDownList("CodContato", String.Empty)
            @Html.ValidationMessageFor(model => model.CodContato)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Numero)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Numero)
            @Html.ValidationMessageFor(model => model.Numero)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.CodOperadora, "Operadora")
        </div>
        <div class="editor-field">
            @Html.DropDownList("CodOperadora", String.Empty)
            @Html.ValidationMessageFor(model => model.CodOperadora)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
